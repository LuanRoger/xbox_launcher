class AppBadgeInfo {
  final bool controllerSupport;
  final bool touchSupport;
  final bool isInGamePass;

  AppBadgeInfo(
      {this.controllerSupport = false,
      this.touchSupport = false,
      this.isInGamePass = false});
}
